# ============================================================
# Heat-Sentinel - Python 3.14 Windows
# Combined Backend + Phase 1 Geospatial Dependencies
# ============================================================

# --- FastAPI ---
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.12,<3
pydantic-settings>=2.7,<3
python-dotenv>=1,<2

# --- HTTP / Weather ---
httpx>=0.28,<1

# --- Thermal / Scientific Computing ---
pythermalcomfort>=4.4,<5
numpy
scipy
numba
llvmlite

# --- Data / ML ---
pandas>=2.3,<3
scikit-learn>=1.7,<2
joblib>=1.5,<2

# --- Explainability ---
shap>=0.46,<0.53

# --- ML Models ---
xgboost>=3.0,<4
lightgbm==4.6.0

# --- Arrow ---
pyarrow>=25,<26

# --- Testing ---
pytest>=8.3,<9

# ============================================================
# Geospatial / Satellite / Phase 1
# ============================================================

geopandas>=1.1,<2
shapely>=2.1,<3
pyproj>=3.8,<4
rasterio>=1.4,<2

# Fiona intentionally omitted:
# Fiona has no suitable CPython 3.14 Windows wheel for this setup
# and would trigger a GDAL source build.

# --- STAC / Microsoft Planetary Computer ---
pystac-client>=0.8,<1
planetary-computer>=1,<2